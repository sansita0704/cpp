#include <iostream>

int main()
{
    const double pi = 3.14;   
    // Use 'const' keyword before variable intialization to convert a variable into a constant.
    // pi = 0;
    // Error: as pi is a constant so its value can't be changed
     
    std::cout << pi;
    return 0;
}
